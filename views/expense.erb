<h1>Categories where your expenses are going to</h1>

<div class = "col-md-6">

  <% if @categories_of_expense.any?() %>
  <ul>
    <% @categories_of_expense.each() do |category| %>
    <li><a href = "/category/<%= category.id() %>"><%= category.name() %></a></li>
    <% end %>
  </ul>
  <% else %>
  <p>There are no categories yet.</p>
  <% end %>

</div>
