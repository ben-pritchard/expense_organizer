<h1>Categories where your expenses are going to</h1>

<div class = "col-md-6">

  <% if @found_expense.categories.any?() %>
  <ul>
    <% @found_expense.categories.each() do |category_id| %>
      <% category = Category.find(category_id) %>
      <li><a href = "/category/<%= category.id() %>"><%= category.name() %></a></li>
    <% end %>
  </ul>
  <% else %>
  <p>There are no categories yet.</p>
  <% end %>

</div>
